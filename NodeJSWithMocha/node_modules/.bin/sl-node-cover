#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sl-node-cover/lib/sl-node-cover" "$@"
  ret=$?
else 
  node  "$basedir/../sl-node-cover/lib/sl-node-cover" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sl-cia/lib/sl-config-cia" "$@"
  ret=$?
else 
  node  "$basedir/../sl-cia/lib/sl-config-cia" "$@"
  ret=$?
fi
exit $ret
